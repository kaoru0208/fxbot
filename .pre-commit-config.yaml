repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.2       # ← Ruff の現在バージョンに合わせる
    hooks:
      - id: ruff       # Lint
        args: [--fix]  # 自動修正
      - id: ruff-format  # フォーマッタ (オプション)
